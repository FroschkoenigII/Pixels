spring:
  application.name: exampleConsumer
  # remove::start[]
  cloud.stream.bindings.input.destination: verifications
  # remove::end[]
server.port: ${PORT:8081}
logging:
  level:
    org.springframework.cloud: debug